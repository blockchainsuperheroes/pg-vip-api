# Database (pg_identity_db on AWS RDS — private subnet)
DB_HOST=172.31.46.190
DB_PORT=5432
DB_NAME=pg_identity_db
DB_USER=backend_pg_account
DB_PASSWORD=changeme

# API Keys
SERVICE_API_KEY=changeme
PG_API_KEY=changeme

# External APIs
ACCOUNT_API_URL=https://api.account.pentagon.games
SERVICE_API_URL=https://api.service.pentagon.games
BCSH_API_URL=https://api.bcsh.xyz

# Pentagon Chain RPC (for on-chain checks)
PENTAGON_RPC=https://rpc.pentagon.games

# Server
PORT=9022
HOST=0.0.0.0

# API key for consumers calling this service
VIP_API_KEY=changeme
